spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/badgedb
    username: badge
    password: badge
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true